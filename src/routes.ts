import Home from "@page/home/home.vue";
import Login from "@page/login/login.vue";

export const routes =
    [
        { path: "/", component: Home },
        { path: "/login", component: Login }
    ];